version: '3'
pipeline:
  
  publish:
    image: docker 
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    commands: 
      - docker version
      #- docker build --tag registry.sindrenerdal.no:5000/hex_to_rgb . 
      #- docker push registry.sindrenerdal.no:5000/hex_to_rgb
  #publish:
      #image: appleboy/drone-ssh
      #host: sindrenerdal.no
      #username: dev
      #key: ${DEPLOY_KEY}
      #port: 22
      #script:
        #- echo hello
 
  #publish:
    #image: plugins/docker 
    #repo: registry.sindrenerdal.no:5000/hex_to_rgb
    #secrets: [docker_username, docker_password] 
  #publish:
    #image: plugins/docker 
    #repo: matmonsen/hex_to_rgb
    #secrets: [docker_username, docker_password]


  #build:
    #image: node:8.1.2-alpine
    #environment: 
      #- SE=${TEST}
    #commands:
     # - echo "heia"
      #- npm install webpack -g
      #- npm install
      #- npm build
    # Only build on master
    #bundle:
     # when:
      # branch: master  
      
      
  
  #deploy:
    #image: nginx:alpine
    #container_name: nginx-hextorgb
    #ports:
      #- "80:80"
    #commands:
      #- cp -a build/. /usr/share/nginx/html/
    # Only deploy on master
    #bundle:
     # when:
       # branch: master  

