version: '3'
pipeline:


  publish:
    image: plugins/docker
    registry: registry.sindrenerdal.no:5000
    repo: registry.sindrenerdal.no:5000/hextorgb
    password: test
    username: test


  #build:
    #image: node:8.1.2-alpine
    #environment:
      #- SE=${TEST}
    #commands:
     # - echo "heia"
      #- npm install webpack -g
      #- npm install
      #- npm build
    # Only build on master
    #bundle:
     # when:
      # branch: master  
      
      
  
  #deploy:
    #image: nginx:alpine
    #container_name: nginx-hextorgb
    #ports:
      #- "80:80"
    #commands:
      #- cp -a build/. /usr/share/nginx/html/
    # Only deploy on master
    #bundle:
     # when:
       # branch: master  

